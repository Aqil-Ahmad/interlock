# Fail fast if someone runs npm/yarn instead of pnpm.
engine-strict=true

# Explicit dependencies only — no phantom resolution through the store.
hoist=false
strict-peer-dependencies=false
save-exact=false

lockfile=true
prefer-frozen-lockfile=true
