/**
 * Dashboard entry point.
 *
 * TODO: mount once React dependencies are installed —
 *   import { createRoot } from 'react-dom/client';
 *   createRoot(document.getElementById('root')!).render(<App />);
 */
export {};
