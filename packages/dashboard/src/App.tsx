/**
 * Dashboard shell.
 *
 * Planned views: branch map (what is in flight, per repo, with dirty state and
 * owning session), pairwise conflict heatmap, finding detail (both diffs, symbol
 * trail, analyzer output), event timeline, recommended landing order.
 */
export function App() {
  return (
    <main>
      <h1>Interlock</h1>
      <p>Dashboard scaffold.</p>
    </main>
  );
}
